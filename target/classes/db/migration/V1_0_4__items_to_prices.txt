CREATE TABLE IF NOT EXISTS PRICES (
price_id serial not null primary key,
price text,
origin text,
origin_id bigint,
updated_at date)

INSERT INTO PRICES
SELECT i.items_id,i.price,i.origin,i.origin_id,i.updated_at FROM ITEMS i

